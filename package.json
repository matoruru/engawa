{
  "name": "kaiwa",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun --cwd packages/server dev",
    "dev:ui": "bun --cwd packages/ui/browser dev",
    "test:server": "bun --cwd packages/server test",
    "build:ui": "bun --cwd packages/ui/browser build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10"
  }
}
